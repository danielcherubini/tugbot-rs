// Moderation commands (gulag, etc.) will be migrated here

use crate::Error;

pub fn commands() -> Vec<poise::Command<crate::Data, Error>> {
    vec![
        // Commands will be added here during migration
    ]
}
